name: Reviewer follow-up PR43

on:
  push:
    branches:
      - codex/phase-b-minimax-m3-reasoning-split

permissions:
  contents: write

jobs:
  patch-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: codex/phase-b-minimax-m3-reasoning-split
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Apply final reviewer follow-up
        run: python scripts/reviewer_patch_pr43_followup.py
      - name: Install Python dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt
      - name: Focused regression tests
        run: |
          python -m unittest tests.test_minimax_provider tests.test_performance_enhanced_regressions -v
          python -m compileall -q app tests
          git diff --check
      - name: Commit final reviewer follow-up
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/providers/minimax_provider.py tests/test_minimax_provider.py
          git commit -m "fix: complete MiniMax reasoning audit hardening"
          git push origin HEAD:codex/phase-b-minimax-m3-reasoning-split
